spring:
  application:
    name: lms-gateway-service
  cloud:
    config:
      enabled: false
  security:
    jwt:
      secret-key: this-is-a-dummy-test-key-that-is-at-least-32-characters-long

eureka:
  client:
    enabled: false